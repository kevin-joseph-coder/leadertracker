# Local database and its WAL sidecars
tracker.db
tracker.db-wal
tracker.db-shm

# Generated by the pipeline - regenerate with `python cohort.py` / `python poller.py`
cohort.json
positions.json
hourly.json
*.tmp

# Scheduled cohort refresh + poller output
cohort.log
poller.log
poller.err.log

__pycache__/
*.py[cod]
.venv/
venv/
